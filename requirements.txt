# FilingAgent — pinned dependencies (Wave 0 contract freeze)
# Versions are pinned to major.minor where feasible; exact patch versions are
# resolved by each lane's environment and re-pinned at integration (Wave 2).

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.9,<3
# 0.5.20's chroma-hnswlib dependency has no Windows wheel and needs MSVC.
# 1.x ships a prebuilt Windows wheel with the same public API and the same
# local ONNX MiniLM default embedding function.
chromadb>=1.5.9,<2
httpx==0.27.2
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.7.1
anthropic==0.39.0
langgraph==0.2.45

# OpenAI-compatible client, used to reach Groq's free tier via a base_url
# override (https://api.groq.com/openai/v1). This is the DEFAULT provider for
# the four arms and the primary judge — see LLM_PROVIDER in .env.example.
# The anthropic pin above is retained as the alternate provider implementation
# behind src/llm.py, so the arms stay portable across both.
openai>=1.54,<2
